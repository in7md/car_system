# دليل النشر السحابي (Cloud Deployment Guide)

هذا الدليل مصمم لرفع مشروعك (نظام إدارة معرض السيارات) للعمل على الإنترنت على مدار الساعة (24/7) باستخدام منصة **Vercel** للاستضافة وقاعدة بيانات **PostgreSQL** سحابية مجانية.

---

## الخطوة 1: إنشاء قاعدة بيانات PostgreSQL سحابية مجانية

تعتبر منصات **Neon.tech** أو **Supabase** من أفضل الخيارات لإنشاء قواعد بيانات PostgreSQL متوافقة مع Vercel. 

### باستخدام Neon.tech (الأسهل والأسرع):
1. اذهب إلى [Neon.tech](https://neon.tech/) وقم بإنشاء حساب مجاني (عبر Google أو GitHub).
2. انقر على **New Project**.
3. قم بتسمية المشروع (مثال: `car-system-db`)، واختر المنطقة الجغرافية الأقرب لك، ثم اضغط **Create Project**.
4. بمجرد الإنشاء، ستظهر لك لوحة تحتوي على رابط الاتصال (Connection String) والذي يبدأ بـ `postgresql://...`.
5. انسخ هذا الرابط بالكامل واحتفظ به. سيكون هو الـ `DATABASE_URL` الخاص بك.

*(ملاحظة: في Neon يمكنك إضافة `?sslmode=require` في نهاية الرابط إذا لم تكن موجودة لضمان الأمان).*

---

## الخطوة 2: رفع المشروع إلى مستودع GitHub

ستقوم Vercel بسحب الكود الخاص بك من GitHub لتشغيله.

1. قم بإنشاء مستودع (Repository) جديد في حسابك على [GitHub](https://github.com/new). اختر أن يكون **Private** للحفاظ على سرية الكود.
2. افتح الـ Terminal (موجه الأوامر) داخل مجلد المشروع في جهازك.
3. نفّذ الأوامر التالية لرفع الكود (إذا لم يكن متصلاً بـ Git بالفعل):
   ```bash
   git init
   git add .
   git commit -m "Initial commit - Ready for production"
   git branch -M main
   git remote add origin https://github.com/USERNAME/REPOSITORY_NAME.git
   git push -u origin main
   ```
*(استبدل USERNAME باسمك و REPOSITORY_NAME باسم المستودع الذي أنشأته).*

---

## الخطوة 3: استيراد المشروع في منصة Vercel وإعداد المتغيرات

1. اذهب إلى منصة [Vercel](https://vercel.com/) وسجل الدخول باستخدام حساب GitHub الخاص بك.
2. من لوحة التحكم، انقر على **Add New...** ثم اختر **Project**.
3. ستظهر لك قائمة بمستودعاتك على GitHub. ابحث عن المستودع الذي رفعته للتو واضغط على زر **Import**.
4. **متغيرات البيئة (Environment Variables):**
   قبل الضغط على Deploy، قم بتوسيع قسم **Environment Variables** وأضف المتغيرات التالية:

   | اسم المتغير (Key) | القيمة (Value) | ملاحظات |
   | --- | --- | --- |
   | `DATABASE_URL` | الصق الرابط الذي حصلت عليه من Neon | رابط قاعدة البيانات السحابية |
   | `NEXTAUTH_URL` | اترك هذا الحقل فارغاً في Vercel | تقوم Vercel بإعداد هذا المتغير تلقائياً، ولكن يمكنك إضافة رابط Vercel الخاص بك لاحقاً |
   | `NEXTAUTH_SECRET` | اكتب نصاً عشوائياً قوياً (مثال: `my-super-secret-key-123!@#`) | ضروري لتشفير جلسات تسجيل الدخول |

5. اضغط على **Deploy** وانتظر بضع دقائق حتى تنتهي Vercel من البناء والرفع.

---

## الخطوة 4: زراعة الحسابات الافتراضية في قاعدة البيانات السحابية (Seed)

بعد اكتمال النشر بنجاح على Vercel، قاعدة البيانات السحابية ستكون مبنية وجاهزة (حيث يقوم نظام Vercel بتشغيل `prisma generate` تلقائياً بفضل إعدادنا للـ `postinstall`).
لكننا بحاجة إلى زرع الأدوار وحسابات الاختبار بداخلها لأول مرة حتى نتمكن من الدخول.

1. افتح مشروعك في جهازك (Local Machine).
2. افتح ملف `.env` الخاص بك واستبدل الـ `DATABASE_URL` المحلي بالرابط السحابي (رابط Neon الذي استخدمته في Vercel) **مؤقتاً**.
3. نفّذ الأمر التالي في التيرمنال الخاص بجهازك لدفع بنية الجداول إلى السحابة:
   ```bash
   npx prisma db push
   ```
4. ثم نفّذ أمر الـ Seed لزراعة المستخدمين:
   ```bash
   npm run db:seed
   ```
5. بمجرد نجاح هذه الخطوة، **أعد الرابط المحلي** إلى ملف `.env` في جهازك كي لا تتأثر قاعدة بيانات الإنتاج بتجاربك المحلية.

---

## الخطوة 5: الدخول إلى النظام السحابي 🚀

الآن، افتح الرابط المخصص لمشروعك والذي أعطتك إياه Vercel (مثال: `https://car-system-beta.vercel.app`).
يمكنك تسجيل الدخول باستخدام حسابات الاختبار التي تم زرعها للتو، على سبيل المثال:
- **البريد:** `owner@test.com`
- **الرقم السري:** `password123`

**تهانينا! نظامك الآن يعمل بشكل مثالي في بيئة سحابية آمنة وعلى مدار الساعة.**
